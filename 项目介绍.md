# 基于AI的大学生职业规划智能体项目介绍

## 项目概述

本项目是一个基于AI的大学生职业规划智能体平台，旨在帮助大学生进行职业规划、岗位探索、简历分析等功能。项目采用前后端分离架构，前端使用Vue 3 + TypeScript，后端使用FastAPI + Python，集成了Neo4j图数据库用于知识图谱构建。

## 项目结构

```
A13/
├── .vscode/               # VS Code配置文件
├── shuju/                # 数据文件目录
│   └── a13基于AI的大学生职业规划智能体-JD采样数据.xls  # 岗位数据Excel文件
├── 前端/                 # 前端项目
│   └── vue-A13/          # Vue 3 + TypeScript项目
│       ├── public/        # 静态资源
│       ├── src/           # 源代码
│       │   ├── api/       # API请求模块
│       │   ├── assets/    # 静态资源
│       │   ├── components/ # 组件
│       │   ├── composables/ # 组合式API
│       │   ├── router/    # 路由配置
│       │   ├── stores/    # Pinia状态管理
│       │   ├── types/     # TypeScript类型定义
│       │   ├── utils/     # 工具函数
│       │   ├── views/     # 页面组件
│       │   ├── App.vue    # 根组件
│       │   └── main.ts    # 入口文件
│       └── package.json   # 依赖配置
└── 后端/                 # 后端项目
    ├── crud/             # 数据操作层
    ├── models/           # 数据模型
    ├── routers/          # 路由
    ├── schemas/          # 数据验证
    ├── uploads/          # 上传文件
    ├── main.py           # 后端入口
    └── requirements.txt  # 依赖配置
```

## 前端部分

### 核心文件结构

- **src/api/**: 包含与后端API通信的模块
  - agent.ts: 智能体相关API
  - career.ts: 职业规划相关API
  - job.ts: 岗位相关API
  - student.ts: 学生相关API
  - user.ts: 用户相关API

- **src/components/**: 组件目录
  - career/: 职业规划相关组件
  - common/: 通用组件
  - job/: 岗位相关组件
  - student/: 学生相关组件

- **src/stores/**: Pinia状态管理
  - agent.ts: 智能体状态管理
  - career.ts: 职业规划状态管理
  - job.ts: 岗位状态管理
  - student.ts: 学生状态管理
  - user.ts: 用户状态管理

- **src/views/**: 页面组件
  - LoginView.vue: 登录页面
  - HomeView.vue: 首页
  - JobExplore.vue: 岗位探索页面
  - StudentProfile.vue: 学生画像页面
  - CareerPlan.vue: 职业规划页面
  - 等其他功能页面

### 核心功能

1. **用户认证**：登录、注册功能
2. **岗位探索**：岗位搜索、筛选、申请、收藏
3. **学生画像**：个人信息管理、简历上传、能力分析
4. **职业规划**：职业路径规划、行动计划制定
5. **智能问答**：基于大模型的职业咨询
6. **知识图谱**：岗位关系可视化

### 核心代码实现

#### 前端入口配置 (main.ts)

```typescript
import './assets/main.css'
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'

const app = createApp(App)
const pinia = createPinia()

app.use(pinia)
app.use(router)
app.use(ElementPlus)

// 初始化 stores 数据
const userStore = useUserStore()
const jobStore = useJobStore()
// ... 其他store初始化

// 应用启动时就开始加载数据
jobStore.fetchJobs()

app.mount('#app')
```

#### 路由配置 (router/index.ts)

```typescript
const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    { path: '/login', name: 'login', component: () => import('../views/LoginView.vue') },
    { path: '/', name: 'home', component: HomeView },
    { path: '/job-explore', name: 'job-explore', component: () => import('../views/JobExplore.vue') },
    // ... 其他路由
  ],
})

// 路由守卫
router.beforeEach(async (to, from) => {
  const userStore = useUserStore()
  
  // 每次启动或刷新页面时强制要求重新登录
  if (from.name === null) {
    userStore.token = null
    userStore.user = null
    localStorage.removeItem('token')
    // ... 清除登录状态
    
    if (to.path !== '/login') {
      window.location.href = '/login'
      return false
    }
  }
  
  // 检查是否需要认证
  if (to.meta.requiresAuth !== false && !userStore.isLoggedIn) {
    if (to.path !== '/login') {
      return '/login'
    }
  }
  
  return true
})
```

## 后端部分

### 核心文件结构

- **models/**: 数据模型
  - user.py: 用户模型
  - student_profile.py: 学生画像模型
  - job.py: 岗位模型
  - application.py: 申请记录模型
  - favorite.py: 收藏模型

- **routers/**: 路由
  - auth.py: 认证相关路由
  - student_profiles.py: 学生画像相关路由
  - job.py: 岗位相关路由
  - career.py: 职业规划相关路由
  - agent.py: 智能体相关路由
  - neo4j.py: 知识图谱相关路由

- **schemas/**: 数据验证
  - user.py: 用户相关DTO
  - student_profile.py: 学生画像相关DTO
  - job.py: 岗位相关DTO
  - agent.py: 智能体相关DTO

### 核心功能

1. **用户管理**：注册、登录、个人信息管理
2. **岗位管理**：岗位列表、详情、申请、收藏
3. **学生画像**：个人信息、简历上传、能力分析
4. **职业规划**：职业路径、行动计划
5. **智能问答**：基于大模型的职业咨询
6. **知识图谱**：岗位关系图谱构建与查询

### 核心代码实现

#### 后端入口配置 (main.py)

```python
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from database import engine, Base
from models import User, StudentProfile, Job, Application, Favorite
from routers import auth, student_profiles, student, job, career, agent, neo4j
from routers.admin import router as admin_router
from fastapi.middleware.cors import CORSMiddleware

Base.metadata.create_all(bind=engine)

app = FastAPI(
    title="职业规划智能体 API",
    version="1.0",
    description="基于AI的大学生职业规划平台，提供用户认证、学生档案管理、岗位搜索、职业规划、智能问答等功能。",
)

os.makedirs("uploads/resumes", exist_ok=True)
app.mount("/uploads", StaticFiles(directory="uploads"), name="uploads")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 注册路由
app.include_router(auth)
app.include_router(student_profiles)
app.include_router(student)
app.include_router(job)
app.include_router(career)
app.include_router(agent)
app.include_router(neo4j.router)
app.include_router(admin_router)

@app.get("/")
def root():
    return {"message": "app 运行成功"}
```

## 技术栈

### 前端
- Vue 3 + TypeScript
- Pinia (状态管理)
- Vue Router (路由)
- Element Plus (UI组件库)
- ECharts (数据可视化)

### 后端
- Python 3.10+
- FastAPI (Web框架)
- SQLite (数据库)
- Neo4j (图数据库)
- LangChain (LLM集成)
- DashScope API (大模型)

## 核心功能实现

### 1. 岗位探索

**前端实现**：
- `JobExplore.vue` 页面展示岗位列表
- `JobCard.vue` 组件展示单个岗位信息
- `JobProfile.vue` 组件展示岗位详情
- `JobGraph.vue` 组件展示岗位知识图谱

**后端实现**：
- `job.py` 路由处理岗位相关请求
- 支持岗位列表、详情、搜索、申请、收藏等操作

### 2. 学生画像

**前端实现**：
- `StudentProfile.vue` 页面管理学生信息
- `ProfileForm.vue` 组件编辑个人信息
- `ResumeUpload.vue` 组件上传简历
- `AbilityRadar.vue` 组件展示能力雷达图

**后端实现**：
- `student_profiles.py` 路由处理学生画像相关请求
- 支持个人信息管理、简历上传、能力分析等功能

### 3. 智能问答

**前端实现**：
- `FloatingAgent.vue` 组件提供悬浮聊天界面
- `agent.ts` store管理对话状态

**后端实现**：
- `agent.py` 路由处理智能问答请求
- 集成DashScope API进行大模型对话
- 支持知识图谱查询

### 4. 知识图谱

**前端实现**：
- `JobGraph.vue` 组件使用ECharts展示知识图谱

**后端实现**：
- `neo4j.py` 路由处理知识图谱相关请求
- 集成Neo4j图数据库
- 支持Cypher查询生成和执行

## 部署说明

### 前端部署
1. 进入前端目录：`cd 前端/vue-A13`
2. 安装依赖：`npm install`
3. 构建项目：`npm run build`
4. 部署dist目录到服务器

### 后端部署
1. 进入后端目录：`cd 后端`
2. 安装依赖：`pip install -r requirements.txt`
3. 启动服务：`python main.py` 或 `./start.bat`

## 项目特色

1. **AI驱动**：集成大模型提供智能职业咨询
2. **知识图谱**：使用Neo4j构建岗位关系网络
3. **个性化推荐**：基于学生画像提供定制化职业建议
4. **全栈架构**：前后端分离，技术栈现代化
5. **用户友好**：响应式设计，良好的用户体验

## 后续优化方向

1. 增强AI模型的职业咨询能力
2. 扩展知识图谱的覆盖范围
3. 增加更多职业测评工具
4. 优化前端性能和用户体验
5. 增强数据安全和隐私保护
